/** Progress UI (Home block, profile menu, /profile/progress). API/domain code stays for re-enable. */
export const PROGRESS_FEATURE_ENABLED = false;

export function isProgressFeatureEnabled(): boolean {
  return PROGRESS_FEATURE_ENABLED;
}
